# Root Python dependencies (backend)
#
# Source of truth: `usv-events-platform/backend/requirements.txt` (used by `docker/Dockerfile.backend`).
# This file is provided for convenience when installing from repo root.

# runtime (API)
fastapi==0.115.6
uvicorn[standard]==0.30.6

# database
SQLAlchemy==2.0.36
alembic==1.13.3
# Postgres driver (needed when DATABASE_URL points to Supabase Postgres)
psycopg[binary]==3.2.3

# validation / settings
pydantic==2.9.2
pydantic-settings==2.6.1

# auth/security
email-validator==2.2.0
python-multipart==0.0.12
bcrypt==5.0.0
python-jose[cryptography]==3.3.0

# reports / integrations
reportlab==4.2.5
httpx==0.27.2

# AI/LLM
# (no external AI SDKs required by current code; see backend/app/ai/)

# scraping/ingestion
# (no external scraping deps required by current code; ingestion is mock-only by default)

# testing
pytest==8.3.3
