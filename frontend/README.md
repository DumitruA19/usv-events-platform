# Frontend (React + Vite)

## Run
```powershell
npm install
npm run dev
```

Set `VITE_API_BASE_URL` in `.env` if your backend differs.

## UI
- Romanian default + English toggle (header).
- Light default + Dark toggle (header).
